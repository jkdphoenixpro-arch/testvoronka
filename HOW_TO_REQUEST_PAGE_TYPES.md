# Как указывать типы страниц при создании

## 📋 Формат запроса

При создании или изменении страниц используйте следующий формат:

```
Создать страницу [маршрут] с типом [pageType] [+ дополнительные параметры]
```

---

## 🎯 Доступные типы страниц (pageType)

### 1. **splash**
Splash экран с логотипом приложения

**Пример запроса:**
```
Создать страницу goal/1 с типом splash
```

**Обязательные параметры:**
- title
- subtitle
- showContinueButton: true

---

### 2. **single-select**
Одиночный выбор с автопереходом после клика

**Пример запроса:**
```
Создать страницу goal/4new с типом single-select
```

**Обязательные параметры:**
- title
- options (массив с value и text)
- autoNavigate: true
- autoNavigateDelay: 500 (или другое значение в мс)

**Характеристики:**
- При клике на опцию → выделяется → автоматический переход через заданную задержку
- Нет кнопки Continue
- Используется для вопросов с одним ответом

---

### 3. **multi-select**
Множественный выбор с иконками и кнопкой Continue

**Пример запроса:**
```
Создать страницу goal/4 с типом multi-select
```

**Обязательные параметры:**
- title
- options (массив с value, text, icon)
- isMultiSelect: true
- showContinueButton: true

**Характеристики:**
- Можно выбрать несколько опций
- Кнопка Continue активируется когда выбрана хотя бы одна опция
- Каждая опция имеет иконку

---

### 4. **multi-pill**
Множественный выбор с pill-кнопками (до 3 выборов)

**Пример запроса:**
```
Создать страницу user/1 с типом multi-pill, максимум 3 выбора
```

**Обязательные параметры:**
- title
- subtitle
- options (массив с value, text, width)
- maxSelections: 3
- showContinueButton: true

**Характеристики:**
- Pill-кнопки разной ширины
- Ограничение на количество выборов (обычно 3)
- Grid layout

---

### 5. **testimonial-grid**
Сетка отзывов с процентами

**Пример запроса:**
```
Создать страницу goal/6 с типом testimonial-grid
```

**Обязательные параметры:**
- title
- subtitle
- testimonials (массив с percentage и description)
- showContinueButton: true

**Характеристики:**
- Grid из карточек
- Каждая карточка имеет процент и описание

---

### 6. **testimonial-advanced**
Отзыв с before/after изображением

**Пример запроса:**
```
Создать страницу user/7 с типом testimonial-advanced
```

**Обязательные параметры:**
- title
- imageSrc (путь к изображению)
- testimonials (массив с description, author, age, rating)
- showContinueButton: true

**Характеристики:**
- Большое изображение before/after
- Отзыв с аватаром, именем, возрастом
- Рейтинг в звездах

---

### 7. **chart**
График с иконкой или Lottie анимацией

**Пример запроса 1 - с Lottie анимацией:**
```
Создать страницу goal/7 с типом chart
```

**Пример запроса 2 - со статичным изображением:**
```
Создать страницу goal/new5 с типом chart,
изображение /image/age-rewind.webp,
текст info-block с иконкой /image/info-icon.svg,
background белый, текст 16px
```

**Обязательные параметры:**
- title
- subtitle
- showContinueButton: true

**Опциональные параметры:**
- chartImage (путь к изображению) - если указан, используется вместо Lottie
- infoText (текст в info-block)
- infoIcon (иконка в info-block, по умолчанию /image/znak.svg)
- animationName (для Lottie)

**Два варианта стилей info-block:**

1. **Стандартный** (для goal/7 и других):
   - Background: `rgba(77, 54, 138, 0.1)` (светло-фиолетовый)
   - Иконка: `/image/znak.svg`
   - Текст: 14px

2. **Альтернативный** (для goal/new5 - добавьте класс `goal-page-new5`):
   - Background: `white`
   - Иконка: `/image/info-icon.svg` (выровнена по центру)
   - Текст: 16px
   - Chart-block: без background и box-shadow

---

### 8. **info**
Информационная страница с изображением и описанием

**Пример запроса:**
```
Создать страницу lifestyle/4 с типом info
```

**Обязательные параметры:**
- title
- description
- imageSrc
- showContinueButton: true

---

### 9. **scale**
Шкала от 1 до 5 для оценки утверждения

**Пример запроса:**
```
Создать страницу statements/1 с типом scale
```

**Обязательные параметры:**
- question
- statement
- autoNavigate: true
- autoNavigateDelay: 500

**Характеристики:**
- 5 кнопок со значениями 1-5
- Автопереход после выбора
- Нет title (используется question и statement)

---

## ✅ Примеры правильных запросов

### Пример 1: Создать простую single-select страницу
```
Создать страницу goal/4new с типом single-select, 
заголовок "Great! Which areas do you want to focus on first?",
опции: Face & neck, Belly & waist, Back & posture,
автопереход через 500ms на goal/5
```

### Пример 2: Создать multi-pill страницу
```
Создать страницу user/8 с типом multi-pill,
заголовок "Select problem areas",
subtitle "Select up to 3 options",
максимум 3 выбора,
опции с разными ширинами pill-кнопок
```

### Пример 3: Создать testimonial-grid
```
Создать страницу goal/10 с типом testimonial-grid,
заголовок "Amazing results",
отзывы: 
- 90% reported improvement
- 85% felt younger
с кнопкой Continue
```

### Пример 4: Скопировать существующую страницу
```
Создать страницу user/10 как копию user/7 (testimonial-advanced),
изменить только заголовок на "New testimonial"
```

---

## 🔧 Дополнительные параметры

При создании страницы можно указать:

### Навигация:
- `переход на [stepId]` - указать следующий шаг
- `автопереход через [число]ms` - задержка перед переходом

### Стили:
- `использовать стили от [legacyClassName]` - применить существующие стили
- `новый CSS класс [className]` - создать новый класс

### Предзагрузка:
- `предзагрузить изображения: [список путей]`

### Сохранение данных:
- `сохранять выбор в [saveKey]`

---

## ❌ Что НЕ нужно указывать

- ID страницы (генерируется автоматически)
- Точные значения CSS (используются существующие)
- Внутренние параметры React компонентов

---

## 📝 Шаблон полного запроса

```
Создать страницу [route] с типом [pageType]
Заголовок: [title]
Подзаголовок: [subtitle] (опционально)
Опции: [список опций]
[Особенности типа страницы]
Переход на: [nextStepId]
Использовать стили: [legacyClassName] (опционально)
```

---

## 🎯 Краткая справка по типам

| Тип | Описание | Автопереход | Continue кнопка |
|-----|----------|-------------|-----------------|
| `splash` | Стартовый экран | ❌ | ✅ |
| `single-select` | Одиночный выбор | ✅ | ❌ |
| `multi-select` | Множественный с иконками | ❌ | ✅ |
| `multi-pill` | Pill-кнопки до 3 шт | ❌ | ✅ |
| `testimonial-grid` | Сетка отзывов | ❌ | ✅ |
| `testimonial-advanced` | Отзыв с фото | ❌ | ✅ |
| `chart` | График/анимация | ❌ | ✅ |
| `info` | Инфо с картинкой | ❌ | ✅ |
| `scale` | Шкала 1-5 | ✅ | ❌ |

---

## 💡 Советы

1. **Для вопросов с одним ответом** → используйте `single-select`
2. **Для множественного выбора с иконками** → используйте `multi-select`
3. **Для множественного выбора без иконок** → используйте `multi-pill`
4. **Для оценки утверждений** → используйте `scale`
5. **Для информационных экранов** → используйте `info` или `chart`
6. **Для отзывов** → используйте `testimonial-grid` или `testimonial-advanced`

---

## 📚 Ссылки

- Подробное описание всех типов: `ONBOARDING_USAGE_GUIDE.md`
- Конфигурация страниц: `src/config/onboardingConfig.ts`
- Полный анализ структуры: `ONBOARDING_STRUCTURE_ANALYSIS.md`
